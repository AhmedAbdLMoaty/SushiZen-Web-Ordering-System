# 🍣 SushiZen Web Ordering System

This project implements the REST API backend for the SushiZen web-based ordering platform using **Spring Boot**, with API definitions driven by **OpenAPI (Swagger)**.

All API endpoints for users, cart, menu, orders, admin, and kitchen staff are defined in the `sushi-zen-api.yml` file and implemented with simple in-memory logic suitable for prototyping and initial integration.

---

## 📦 Tech Stack

- Java 17
- Spring Boot
- Swagger Codegen
- OpenAPI 3.0
- Maven

---

## 📁 Project Structure

| Folder/File | Description |
|-------------|-------------|
| `sushi-zen-api.yml` | OpenAPI 3.0 spec (main YAML file) |
| `controller/` | Java controllers (User, Cart, Order, Menu, Admin, Kitchen) |
| `model/` | DTOs (auto-generated via Swagger Codegen) |
| `api/` | Interface stubs generated by Swagger Codegen |
| `pom.xml` | Maven configuration for build and dependencies |

---

## 🚀 How to Run the Project

### 1. 🧬 Generate the Spring Boot Server Code (if needed)

Make sure you have Swagger Codegen installed:

npm install -g @openapitools/openapi-generator-cli

```
openapi-generator-cli generate \
  -i sushi-zen-api.yml \
  -g spring \
  -o sushi-zen-backend \
  --library spring-boot \
  --additional-properties=interfaceOnly=true
```


### 2. 🔨 Build the Project

```bash
cd sushi-zen-backend
./mvnw clean install
```

or, if you use Maven directly:

```bash
mvn clean install
```

---

### 3. ▶️ Run the Server

```bash
mvn spring-boot:run
```

Once the server is running, you can access the Swagger UI at:

```
http://localhost:8080/swagger-ui/index.html
```

---

### 4. 🧪 Test the APIs

You can use:
- **Postman** (import the `sushi-zen-api.yml` file)
- **Swagger UI** (auto-hosted with the project)
- **cURL**, **SoapUI**, or your Angular frontend in Lab 3

---

## 📌 Status

✅ All major endpoints are implemented  
✅ Controllers are connected to simple in-memory stores  
✅ Models are generated from YAML DTOs  
✅ Ready for integration with the Angular frontend in the next lab

---

## 👤 Author

Course: Information Systems Modelling (ISM)  
Lab 02 – Spring Boot API Implementation

---

